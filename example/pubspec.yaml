name: amazon_s3_cognito_example
description: Demonstrates how to use the amazon_s3_cognito plugin.
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  amazon_s3_cognito:
    path: ../

flutter:
  uses-material-design: true